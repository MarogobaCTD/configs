server:
  port: 8080
spring:
  cloud:
    gateway:
      default-filters:
        - name: LogFilter
      routes:
        - id: catalogRoute
          uri: lb://CATALOG-SERVICE
          predicates:
            - Path=/catalog-api/**
        - id: MOVIE-SERVICE
          uri: lb://MOVIE-SERVICE
          predicates:
            - Path=/movie-api/**
      discovery:
        enabled: true 
